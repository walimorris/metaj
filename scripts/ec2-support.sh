# Some api endpoints rely on EC2 meta-data and the following commands will assist with installing
# needed dependencies.

# Needed for Ubuntu and other Linux flavors for querying instance meta data
sudo apt-get install cloud-utils
sudo apt install amazon-ec2-utils
